import { Text, View } from "react-native";

export default function Tips_Guidelines() {

  

  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}>
      <Text>Tips_Guidelines</Text>
    </View>
  );
}