import { Text, View } from "react-native";

export default function FoundItems() {

  

  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}>
      <Text>FoundItems</Text>
    </View>
  );
}