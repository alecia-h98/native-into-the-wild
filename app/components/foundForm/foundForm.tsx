import { Text, View } from "react-native";

export default function FoundForm() {

  

  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}>
      <Text>FoundForm</Text>
    </View>
  );
}